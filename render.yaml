services:
  - type: web
    name: tradbot-crm
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/auth/me
    envVars:
      - key: JWT_SECRET
        generateValue: true
